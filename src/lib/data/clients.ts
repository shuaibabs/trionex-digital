import type { Client, PlaceholderImage } from '../types';
import placeholderData from '../placeholder-images/clients.json';
export const placeholderImages: PlaceholderImage[] = placeholderData.clientsImages;
export const clients: Client[] = [
  // { id: '1', name: 'Company Name', imageId: 'client-logo-1' }
];